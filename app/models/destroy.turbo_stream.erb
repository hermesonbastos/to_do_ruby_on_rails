<%= turbo_stream.remove @column %>

<% if @board.columns.where(is_done_column: true).none? %>
  <%= turbo_stream.append "column-creator-actions" do %>
    <div id="done-column-alert"
         class="alert alert-outline alert-info shadow-sm px-3 py-2 text-sm gap-2">
      <i class="fa-solid fa-circle-info"></i>
      <span class="text-base-content">
        Defina a(s) coluna(s) de tarefas concluídas, isso nos ajuda a metrificar seus resultados.
      </span>
    </div>
  <% end %>
<% end %>